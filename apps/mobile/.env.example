# Only required on a physical device (same Wi‑Fi as your Mac).
# iOS Simulator uses localhost automatically — no .env needed for simulator.
EXPO_PUBLIC_API_URL=http://192.168.1.35:3000
# Android Emulator (optional — defaults to http://10.0.2.2:3000):
# EXPO_PUBLIC_API_URL=http://10.0.2.2:3000
